This library registers the Javascript files for superfish ONLY. You must copy the superfish CSS files to your theme's CSS folder in order to make proper use of it.

The CSS files will automatically be included at render time by the theming engine so long as they are present in the theme CSS folder.